name: openapi_generator
description: Generated API clients for the app.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dio: ^5.7.0           # HTTP client used by the generated code
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.0
  openapi_generator: ^6.0.1  # wraps the Java CLI
  json_serializable: ^6.8.0